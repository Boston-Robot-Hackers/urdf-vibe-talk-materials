I want a python function to visualize the dataa coming from my lidar. The input will be the output from "ros2 topic echo /scan".

Use matplotlib to generate a polar plot showing the data corresponding to the data. The scale should be in radians around the circumferance (where forward is in the North position.) The scale along the radius should be in Meters. There should be dots corresponding to each point. 
